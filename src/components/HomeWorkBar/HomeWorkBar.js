import React from 'react';
import { Jumbotron, Container } from 'reactstrap';

const HomeWorkBar = (props) => {
  return (
    <div class="recentWork">
      <p>Most Recent Work</p>
    </div>
  );
};

export default HomeWorkBar;